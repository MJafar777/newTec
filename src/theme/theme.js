import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#01636C",
      light: "#032955",
    },
    secondary: {
      main: "#032955",
    },
    bgColor: {
      main: "#fff",
    },
    textColor: {
      main: "#9A9693",
    },
  },
});
